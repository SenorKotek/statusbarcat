# Prebuilt ayabar app

В эту папку CI автоматически выкладывает свежий `ayabar.app.zip`,
собранный на `macos-latest` после пуша в `main`.

Файл обновляется автоматически workflow'ом `.github/workflows/build-and-release.yml`.
